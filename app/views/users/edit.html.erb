<div class="pagina">
<div style="position:fixed;padding-left:250px;top:-5px;z-index:99999999;color:white;">
  <h2><b>Edit <%=@user.name%></b></h2>
</div>
<div class="container">
    <h1>Edit User</h1>
    <%= simple_form_for(@user, url: user_path(@user.id), html: { method: :put }) do |f| %>
        <strong>Email Assigned to Account: </strong> <%= @user.email %>
        <p><%= f.input :name, placeholder: "Name", :label => "Account Name", :class => 'text-right', :input_html=> { :class => "form-control" }  %></p>
        <%= f.input :role, as: :string, label: "Check box to make admin:" do %>
            <%= f.check_box :role, {}, "admin", "user" %>
        <% end %>
        <%= f.submit :class => "btn btn-success" %> <%= link_to 'Back', user_path(@user), :class => 'btn btn-default' %>
    <% end %>
    
</div>
</div>